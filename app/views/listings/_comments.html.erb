<div class="comments mb-5">
  <h4 class="my-3 p-4 text-white bg-success d-flex align-items-center"><%= fa_icon "comments", class: 'mr-4 fa-2x' %> Product Questions & Answers</h4>
  <hr>

  <p>
    <%= @listing.comments.count %> Questions Asked...</p>
  <%= render @listing.comments %>

  <hr>
  <% if current_user != @listing.user %>
  <p class="lead">Ask a Question:</p>
  <%= render 'comments/form' %>
  <% end %>
 </div>
